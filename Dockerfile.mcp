FROM python:3.11-slim
WORKDIR /app

COPY requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONUNBUFFERED=1
ENV PORT=8001
EXPOSE 8001

CMD ["uvicorn", "mcp_server_sse:app", "--host", "0.0.0.0", "--port", "8001", "--workers", "1"]
